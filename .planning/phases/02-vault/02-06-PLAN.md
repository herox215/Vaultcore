---
phase: 02-vault
plan: 06
type: execute
wave: 6
depends_on: ["02-05"]
files_modified:
  - src-tauri/src/commands/files.rs
  - src/components/Sidebar/Sidebar.svelte
  - src/components/Editor/EditorPane.svelte
autonomous: true
gap_closure: true
requirements:
  - FILE-03
  - SYNC-06
  - SYNC-08

must_haves:
  truths:
    - "RenameResult serializes fields as camelCase (newPath, linkCount) matching TypeScript expectations"
    - "TreeNode receives handlePathChanged function via onPathChanged prop, enabling sidebar refresh after rename"
    - "External-change merge toast displays German text: Externe Aenderungen wurden in <file> eingebunden"
    - "Conflict toast displays German text: Konflikt in <file> – lokale Version behalten"
  artifacts:
    - path: "src-tauri/src/commands/files.rs"
      provides: "RenameResult with camelCase serde"
      contains: "serde(rename_all"
    - path: "src/components/Sidebar/Sidebar.svelte"
      provides: "onPathChanged wired to handlePathChanged"
      contains: "onPathChanged={handlePathChanged}"
    - path: "src/components/Editor/EditorPane.svelte"
      provides: "German toast messages for merge and conflict"
      contains: "Externe Änderungen wurden in"
  key_links:
    - from: "src-tauri/src/commands/files.rs"
      to: "TypeScript rename_file IPC consumer"
      via: "serde JSON serialization"
      pattern: 'rename_all.*camelCase'
    - from: "src/components/Sidebar/Sidebar.svelte"
      to: "handlePathChanged function"
      via: "onPathChanged prop on TreeNode"
      pattern: "onPathChanged=.handlePathChanged."
---

<objective>
Close 3 blocker gaps from Phase 02 verification: (1) RenameResult serde field name mismatch breaking wiki-link count prompt, (2) undefined onPathChanged prop preventing sidebar refresh after rename, (3) English toast messages where German is required by ROADMAP SC#4.

Purpose: These 3 single-line fixes unblock ROADMAP Success Criteria #2 and #4 for Phase 02.
Output: Three corrected source files passing all verification checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vault/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix RenameResult serde and Sidebar onPathChanged wiring</name>
  <files>src-tauri/src/commands/files.rs, src/components/Sidebar/Sidebar.svelte</files>
  <read_first>
    - src-tauri/src/commands/files.rs (lines 276-283 — RenameResult struct definition)
    - src/components/Sidebar/Sidebar.svelte (lines 150-158 for handlePathChanged, lines 204-215 for TreeNode usage)
  </read_first>
  <action>
**Fix CR-01 — RenameResult serde mismatch (src-tauri/src/commands/files.rs):**

On line 278, change:
```rust
#[derive(serde::Serialize)]
```
to:
```rust
#[derive(serde::Serialize)]
#[serde(rename_all = "camelCase")]
```

This ensures `new_path` serializes as `newPath` and `link_count` serializes as `linkCount`, matching what the TypeScript IPC consumer reads.

**Fix CR-02 — undefined onPathChanged prop (src/components/Sidebar/Sidebar.svelte):**

On line 213, change:
```svelte
{onPathChanged}
```
to:
```svelte
onPathChanged={handlePathChanged}
```

This passes the actual `handlePathChanged` function (defined on line 156) to each TreeNode, so sidebar refreshes after rename.
  </action>
  <verify>
    <automated>cd /home/sokragent/Projects/vaultcore && grep -n 'rename_all.*camelCase' src-tauri/src/commands/files.rs && grep -n 'onPathChanged={handlePathChanged}' src/components/Sidebar/Sidebar.svelte && cd src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep 'rename_all.*camelCase' src-tauri/src/commands/files.rs returns a match on the line immediately before or on the RenameResult struct definition
    - grep 'onPathChanged={handlePathChanged}' src/components/Sidebar/Sidebar.svelte returns exactly one match
    - grep '{onPathChanged}' src/components/Sidebar/Sidebar.svelte returns zero matches (shorthand form removed)
    - cargo check completes without errors
  </acceptance_criteria>
  <done>RenameResult serializes as camelCase JSON; TreeNode receives handlePathChanged via onPathChanged prop; cargo check passes</done>
</task>

<task type="auto">
  <name>Task 2: Replace English merge/conflict toasts with German text</name>
  <files>src/components/Editor/EditorPane.svelte</files>
  <read_first>
    - src/components/Editor/EditorPane.svelte (lines 260-280 — merge result handling with toast messages)
    - .planning/phases/02-vault/02-CONTEXT.md (specifics section — required German strings)
  </read_first>
  <action>
**Fix Gap 3 — German toast text (src/components/Editor/EditorPane.svelte):**

On line 271, change the message string from:
```
`External changes merged into ${filename}.`
```
to:
```
`Externe Änderungen wurden in ${filename} eingebunden.`
```

On line 278, change the message string from:
```
`Conflict in ${filename} — local version kept.`
```
to:
```
`Konflikt in ${filename} – lokale Version behalten.`
```

Note the dash character on line 278: use an en-dash (–, U+2013), not an em-dash.
  </action>
  <verify>
    <automated>cd /home/sokragent/Projects/vaultcore && grep -n 'Externe Änderungen wurden in' src/components/Editor/EditorPane.svelte && grep -n 'Konflikt in' src/components/Editor/EditorPane.svelte && grep -c 'External changes merged' src/components/Editor/EditorPane.svelte</automated>
  </verify>
  <acceptance_criteria>
    - grep 'Externe Änderungen wurden in' src/components/Editor/EditorPane.svelte returns exactly one match containing "${filename} eingebunden."
    - grep 'Konflikt in' src/components/Editor/EditorPane.svelte returns exactly one match containing "${filename} – lokale Version behalten."
    - grep -c 'External changes merged' src/components/Editor/EditorPane.svelte returns 0 (no English merge toast remaining)
    - grep -c 'local version kept' src/components/Editor/EditorPane.svelte returns 0 (no English conflict toast remaining)
  </acceptance_criteria>
  <done>Both merge toasts display German text matching ROADMAP SC#4 requirements; no English toast strings remain in merge handling</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced — these are single-line fixes to existing code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | I (Information Disclosure) | RenameResult serde | accept | Adding camelCase rename attribute does not change data content, only field name casing in JSON serialization — no new information exposed |
</threat_model>

<verification>
After both tasks complete:

1. `cargo check` passes (Rust compilation valid after serde attribute addition)
2. `grep -c 'rename_all.*camelCase' src-tauri/src/commands/files.rs` returns 1+
3. `grep -c 'onPathChanged={handlePathChanged}' src/components/Sidebar/Sidebar.svelte` returns 1
4. `grep -c 'Externe Änderungen' src/components/Editor/EditorPane.svelte` returns 1
5. `grep -c 'Konflikt in' src/components/Editor/EditorPane.svelte` returns 1
6. `grep -c 'External changes merged' src/components/Editor/EditorPane.svelte` returns 0
</verification>

<success_criteria>
All 3 verification gaps from 02-VERIFICATION.md are closed:
- CR-01: RenameResult fields serialize as camelCase, enabling wiki-link count prompt
- CR-02: onPathChanged prop correctly passes handlePathChanged to TreeNode, enabling sidebar refresh after rename
- SC#4: Merge and conflict toasts display German text matching ROADMAP success criteria
</success_criteria>

<output>
After completion, create `.planning/phases/02-vault/02-06-SUMMARY.md`
</output>
